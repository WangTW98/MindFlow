# Code evidence

Prefer executable evidence: route declarations, component composition, event bindings, handler bodies, store transitions, API calls, permission guards, and lifecycle registrations. Use `path:line` plus symbol when available. Treat names, comments, dead code, mocks, fixtures, and tests as supporting rather than conclusive evidence unless the runtime contract depends on them.

