


1，左上角按钮组新增：状态组icon按钮，点击此按钮控制状态组列表弹窗的显示隐藏，列表弹窗具有新增按钮，列表项为：颜色（随机创建纯色，以正方形形式展示）、标题、唯一id（隐藏不显示）、删除icon按钮；
2，所有节点卡片右侧详情栏加入状态组下拉选择（默认为无）；
3，所有节点卡片加入状态组展示（展示颜色、状态组名称）；
4，点击状态组列表中的删除icon按钮，删除对应的状态组数据，同时也修改已关联此状态组的节点卡片中的状态为默认（无）；



1，移除左上角按钮组的自动排版按钮及其相关功能，在MCP编辑画布阶段，生成的节点应具有合理的布局结构；
2，状态组的定位：区分同一个页面/弹窗的不同状态（如支付状态弹窗可能具有支付中/待支付/支付成功/支付失败，他们仅在文案或图片上不同，但布局完全一致），是可选项，AI不能为了填充数据而随意编造；
3，除非应用端为单页逻辑，否则从应用端卡片开始的第一个节点卡片应描述当前应用端的导航结构（通过功能组、功能项的嵌套实现父子嵌套导航的展示），再由不同的功能组/功能项（对应父子导航）连接到后续的节点卡片；
4，AI通过MCP绘制节点时，应根据实际业务逻辑使用正确的连线类型（蓝、绿、紫、粉）以正确表明状态、交互、数据流转（但不要为了实际情况而出现四种连线同时连接相同的起始节点，这样会导致连线重合，若出现4种连线同时连接相同起始点的情况，应取舍为一种连线类型并在连线描述中写明多种状态的描述）；





1，分析pencil mcp，结合当前mindflow完整功能，以表格形式列出其全部能力并给出当前mindflow可借鉴的功能

1，只有 webview 选择态和 flow 读写，没有 MCP 级 editor state，增加 mindflow_get_editor_state，返回当前 flow、选中节点/边/分类法、schema、编辑规则；
2，当前提示词硬编码在 src/prompts，改为增加 flow/PRD/Pencil 生成指南库，支持不同产品类型、复杂度、输出风格；
3，read_flow 返回整份 flow，节点/边没有批量查询工具，增加 mindflow_batch_get：按节点/边/应用端/角色/业务域/状态/产物状态查询，支持 depth 和 artifact 展开；
4，MindFlow 有 propose_change + apply_change_plan，以及单项 CRUD，增加 mindflow_batch_edit，一次提交多类节点/边/分类法/布局/产物操作，支持 dry-run 和问题报告；
5，增加 mindflow_snapshot_layout(problemsOnly)，检查卡片重叠、边跨越、孤立节点、过远节点、文字溢出，整理各节点位置使其人工可读性更好（布局不叠加，具有合理的间距能展示连线路径；从左向右为应用端-导航-节点卡片，且相同应用端、业务域尽量排列在同一高度下）；
6，增加画布导出，支持选中节点或整张 flow为产品PRD文档；
7，增加画布导出，支持选中节点或整张 flow为产品PRD文档；