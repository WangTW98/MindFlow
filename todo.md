2，移除应用端/业务域/角色独立面板中的编辑和删除按钮（编辑通过选中对应的应用端节点卡片/列表项后在右侧展示应用端详情栏中进行编辑，业务域和角色因不具有节点卡片，所以仅通过选中列表项后在右侧展示详情栏中进行编辑；删除逻辑为选中应用端节点卡片/列表项后按键盘删除del键直接删除节点卡片并移除关联的连线，业务域和角色因不具有节点卡片，所以仅通过选中列表项后按键盘删除del键直接删除，同时移除画布所有节点卡片中关联的业务域/角色）；

1，在VSCode视口右下角创建悬浮按钮组：严重、警告、可选，3个按钮以lucide icon按钮形式展示，且具有信息数量角标，点击按钮切换控制位于底部的信息列表栏的展示隐藏，信息列表栏具有TAB切换，分别展示对应3个按钮状态的信息内容，列表信息行内容为：标题、描述、操作按钮（使用lucide icon的按钮，点击后复制处理提示词到剪贴板，同时toast展示已复制文案）；此功能核心为：AI通过MCP生成mindflow时，会根据实际产品业务推断逻辑闭环、需求优化等相关产品设计问题并按照问题程度分为严重、警告、可选三个等级，用户点击操作按钮将问题相关描述复制到剪贴板并粘贴给agent供其分析并完善当前mindflow；

1，主动暴露MCP Server，能够被本机安装的Codex/Gemini CLI/Claude Code等agent自动检测并识别调用；


1，移除左侧节点卡片列表中的生成完整pencil和生成完整prd按钮；
2，移除右侧节点卡片详情栏右上角的生成完整pencil和生成完整prd按钮；
3，左侧节点卡片列表中的收起展开icon按钮移动到应用端/业务域/角色按钮组中，按钮组纵向排列；
4，收紧左侧节点卡片列表的宽度；
5，应用端/业务域/角色按钮默认关闭列表弹窗而不是总是激活打开；
6，按钮组添加浮动的排版icon按钮，使用lucide icon，点击此按钮后，将根据当前画布中的节点卡片内容，重新整理节点卡片的位置，使其不会堆叠/挤压，同时节点卡片之间会具有合适的间距能清晰呈现连线关系与连线标题；
7，修改画布展示优先级：画布中的连线只会在节点卡片下方穿过，但要注意兼容实际连接点的样式，不能出现连节点处的线段也在卡片下方；
8，节点卡片高度应根据实际内容自动撑开；


1，分析并处理问题：应用端/业务域/角色的列表项无法被新建/删除；列表项超出高度后，滚动逻辑不生效，默认执行了画布的滚动；